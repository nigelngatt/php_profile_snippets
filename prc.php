$dir = scandir(__DIR__);
$r = new ReflectionClass($dir);
string $function_name;
if($p = $r->getParameters() !== true){}
if($r->Core($properties) && ($r->$method === Form->$method)){
 if($r->getReturn() === false){
  return new ($class = $class_name(__CLASS__)::$method());
 } elseif($r->getReturn() === true){
    return new $p->Searcher($function_name);
 } else exit;
}
